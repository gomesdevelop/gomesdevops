apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: production-ingress
  namespace: production
  annotations:
    cert-manager.io/cluster-issuer: "production-letsencrypt"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - "gomes.dev"
    secretName: app-letsencrypt-tls
  - hosts:
    - "api.gomes.dev"
    secretName: api-letsencrypt-tls
  rules:
  - host: gomes.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx-service
            port:
              number: 80
  - host: api.gomes.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx-service
            port:
              number: 80